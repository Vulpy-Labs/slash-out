{
  "name": "slash-out-server",
  "author": "Vulpy Labs",
  "license": "MIT",
  "version": "1.0.0",
  "description": "Slash Out Authoritative Server",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node -r tsconfig-paths/register src/main.ts",
    "start:dev": "ts-node-dev -r tsconfig-paths/register --watch \"src/**/*\" --respawn --transpile-only src/main.ts",
    "start:prod": "node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@colyseus/schema": "^2.0.36",
    "@colyseus/tools": "^0.15.0",
    "colyseus": "^0.15.57",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2"
  }
}
